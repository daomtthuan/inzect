{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Test",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm script playground lifecycle",
      "cwd": "${workspaceFolder}"
    }
  ]
}
